import { Heebo, Open_Sans } from "next/font/google";

export const openSans = Open_Sans({
  subsets: ["latin"],
  fallback: ["sans-serif"],
});

export const heebo = Heebo({
  subsets: ["latin"],
  fallback: ["sans-serif"],
});
