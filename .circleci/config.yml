version: 2.1
jobs:
  build:
    docker:
      - image: maven:3.6.0-jdk-8
    steps:
      - checkout
      - run:
          name: Build the Container
          command: |
            mvn package -PPKG -Dorg.slf4j.simpleLogger.log.org.apache.maven=warn -B
  package_containers:
    docker:
      - image: java:8-alpine
    steps:
        name: Build actual container image for neighbour-discoverer

