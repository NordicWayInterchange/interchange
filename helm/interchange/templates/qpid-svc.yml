kind: Service
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-qpid-secure
  labels:
    app: {{ .Chart.Name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    component: qpid
    release: {{ .Release.Name }}
spec:
  selector:
    app: {{ .Chart.Name }}
    component: qpid
    release: {{ .Release.Name }}
  ports:
  - name: amqps
    port: 5671
    targetPort: amqps
  - name: http-qpid
    port: 8080
    targetPort: http-qpid