apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-postgis
  labels:
    app: {{ .Chart.Name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    component: postgis
    release: {{ .Release.Name }}
spec:
  selector:
    app: {{ .Chart.Name }}
    component: postgis
    release: {{ .Release.Name }}
  ports:
  - name: db
    port: 5432
    targetPort: db
